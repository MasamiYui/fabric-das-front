<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.it611.das.mapper.PhotoMapper">

    <insert id="addPhoto" parameterType="org.it611.das.domain.Photo">
        INSERT INTO tb_photo(id, title, des, owner_id, author, files, fileshash, transaction_id, submit_time, state)
        VALUES (#{id}, #{title}, #{des}, #{ownerId}, #{author}, #{files}, #{filesHash}, #{transactionId}, #{submitTime}, #{state})
    </insert>

    <select id="selectPhotoTotal" resultType="long">
          select count(*)
          from tb_photo
          where owner_id =#{userId}
    </select>

    <select id="selectPhotoList" resultType="hashmap">
        select id, title, des, owner_id, author, files, fileshash, transaction_id, submit_time, state
        from tb_photo
        where owner_id =#{userId}
    </select>

    <select id="selectPhotoDetailById" parameterType="string" resultType="hashmap">
        select id, title, des, owner_id, author, files, fileshash, transaction_id, submit_time, state
        from tb_photo
        <where>
            id=#{id}
        </where>
    </select>

    <select id="selectPhotoRecordById" parameterType="string" resultType="hashmap">
        select id, title, des, owner_id, author, files, fileshash, transaction_id, submit_time, state
        from tb_photo
        <where>
            id=#{value}
        </where>
    </select>

</mapper>